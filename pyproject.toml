[project]
name = "hsk-vocab-loader"
version = "0.1.0"
description = "A loader for HSK vocabulary lists to Quizlet flashcards."
requires-python = ">=3.12"
dependencies = [
    "easyocr>=1.7.2",
    "pdfplumber>=0.11.9",
    "requests>=2.33.1",
]
